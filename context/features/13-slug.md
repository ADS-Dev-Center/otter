## goals

implementing slug to website

### slug

- add slug in the schema database
- push database with new flow
- the slug is auto generate by name

### implementing slug

- change flow in project and credentials to using slug insted id
- delete confirmation using slug to confirmate the deletion
- slug is unique so user canot create the same project, credentials with the same slug

### when is done

- user redirect to page with no error
- user canot duplicate or create the same projects, credentials
